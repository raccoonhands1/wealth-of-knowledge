### Q1
for any finite sets A, B, C, prove
$$
(A \setminus B) \cup (B \setminus C) \cup (C \setminus A) = (A \cup B \cup C) \setminus (A \cap B \cap C)
$$
by showing that $\forall e$, $e \in LHS \leftrightarrow e \in RHS$


assume $e \in LHS$

these statements hold true
$e \in A \setminus B \Rightarrow e \in A \cup B \cup C$ and $e \notin B$ so $e \notin A \cap B \cap C$
$e \in B \setminus C \Rightarrow e \in A \cup B \cup C$ and $e \notin C$ so $e \notin A \cap B \cap C$
$e \in C \setminus A \Rightarrow e \in A \cup B \cup C$ and $e \notin A$ so $e \notin A \cap B \cap C$

thus $e \in RHS$ in all cases

assume $e \in RHS$, so $e \in A \cup B \cup C$ and $e \notin A \cap B \cap C$
at least one of $e \notin A$, $e \notin B$, $e \notin C$ holds since $e \notin A \cap B \cap C$

these statements hold true
$e \notin B$: if $e \in A$ then $e \in A \setminus B$. else $e \in C$ (from union) so $e \in C \setminus A$
$e \notin C$: if $e \in B$ then $e \in B \setminus C$. else $e \notin B$, covered above
$e \notin A$: if $e \in C$ then $e \in C \setminus A$. else $e \in B$ so $e \in B \setminus C$

thus $e \in LHS$ in all cases

### Q2
$n = 2^k$ teams requires $n - 1 = 2^k - 1$ matches since each match eliminates a team

bijection -- $f: \mathcal{O} \to \mathcal{B}$: label matches $1$ to $2^k - 1$ in round order. for match $i$, set $b_i = 0$ if the left-bracket participant wins & $b_i = 1$ otherwise

**injective** -- distinct outcomes differ on some match $i$, so bit strings differ at position $i$

surjective -- any $s \in \mathcal{B}$ determines an outcome match by match. round 1 participants are fixed by the bracket. later rounds follow from prior winners. each match always has exactly 2 participants so every bit assignment is valid

thus $|\mathcal{O}| = |\mathcal{B}| = 2^{2^k - 1}$


### Q3

#### 1)
ok :)

#### 2a)
reflection across $y = x + 1$ sends $(a, b) \rightarrow (b-1, a+1)$
$$
(n, n) \rightarrow (n-1, n+1)
$$

#### 2b)
reflection sends $(x, y) \rightarrow (y-1, x+1)$

- right step $(x,y) \to (x+1, y)$ maps to $(y-1, x+1) \to (y-1, x+2)$ — up step
- up step $(x,y) \to (x, y+1)$ maps to $(y-1, x+1) \to (y, x+1)$ — right step

right/up steps map to up/right steps so the reflected segment stays monotone
                                                                                                                                                       

#'\]]












































?}}}}}}}}}}]]]]]]]]]]### 3
define $g: P(n-1, n+1) \to P_b$ by the same reflection. any $p' \in P(n-1, n+1)$ must cross $y = x+1$ since the endpoint $(n-1, n+1)$ satisfies $n+1 > (n-1)+1$. let $(k, k+1)$ be the first crossing. reflect the segment from $(k, k+1)$ to $(n-1, n+1)$ back across $y = x+1$, yielding a segment ending at $(n, n)$. the prefix stays below $y = x+1$ by minimality, so the result is a bad path

$g \circ f = \text{id}_{P_b}$ and $f \circ g = \text{id}_{P(n-1,n+1)}$ so $f$ is bijective

---

### 4

#### a)
suppose $g(f(x)) = x$ for all $x \in X$. if $f(x_1) = f(x_2)$
$$
x_1 = g(f(x_1)) = g(f(x_2)) = x_2
$$
thus $f$ is injective

#### b)
suppose $f(g(y)) = y$ for all $y \in Y$. let $y \in Y$, set $x = g(y)$
$$
f(x) = f(g(y)) = y
$$
thus $f$ is surjective
